<h1>Network delay time</h1>

<div>
  Given a list of directed edges, figure out how long it takes for
  a signal to propagate from a source vertex to all vertices in the
  graph. If it's not possible, return infinity.
</div>

<%= solutions_table do %>
  Bellman-Ford algorithm   O(|V||E|)  O(|V|)
  Breadth-first search     O(|V||E|)  O(|V| + |E|)
  Depth-first search       O(|V||E|)  O(|V| + |E|)
<% end %>

<section class="solutions">
  <div class="explanation">
    Graph traversal problem. From the source vertex, visit all
    nodes and find the minimum time it takes to visit each node.
    If any nodes remain unvisited, return null. Otherwise, return
    the maximum of the minimum times.
  </div>

  <section class="solution">
    <h2>Solution 1: Bellman-Ford algorithm</h2>
    <%= rb_code "code/problems/network_delay_time/s1.rb" %>
  </section>

  <section class="solution">
    <h2>Solution 2: Breadth-first search</h2>
    <%= rb_code "code/problems/network_delay_time/s2.rb" %>
  </section>

  <section class="solution">
    <h2>Solution 3: Depth-first search</h2>
    <%= rb_code "code/problems/network_delay_time/s3.rb" %>
  </section>
</section>

<%= resources_section do %>
  Leetcode problem
  https://leetcode.com/problems/network-delay-time/
<% end %>
